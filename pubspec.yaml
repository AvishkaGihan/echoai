name: echoai
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase core
  firebase_core: ^4.2.1

  # Firebase services
  firebase_auth: ^6.1.2
  firebase_ai: ^3.5.0

  # State management
  provider: ^6.1.5+1

  # Local storage
  sqflite: ^2.4.2
  path_provider: ^2.1.5

  # Voice Features
  speech_to_text: ^7.3.0
  flutter_tts: ^4.2.3

  # Authentication
  google_sign_in: ^7.2.0

  # Utilities
  uuid: ^4.5.2
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
